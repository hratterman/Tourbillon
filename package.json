{
  "name": "tourbillon",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A physically-grounded, interactive simulation of a minute-repeating tourbillon wristwatch movement.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/three": "^0.185.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
